from unittest import TestCase\nfrom sw_agent.pantry_item import PantryItem\n\nclass TestPantryItem(TestCase):\n    def test_valid_pantry_item(self):\n        item = PantryItem(name="Flour", quantity=2, unit="cups")\n        self.assertEqual(item.name, "Flour")\n        self.assertEqual(item.quantity, 2)\n        self.assertEqual(item.unit, "cups")\n\n    def test_invalid_name(self):\n        with self.assertRaises(ValueError):\n            PantryItem(name="", quantity=1, unit="g")\n\n        with self.assertRaises(ValueError):\n            PantryItem(name=123, quantity=2, unit="cups")\n\n    def test_invalid_quantity(self):\n        with self.assertRaises(ValueError):\n            PantryItem(name="Flour", quantity=-1, unit="cups")\n\n        with self.assertRaises(ValueError):\n            PantryItem(name="Flour", quantity="two", unit="cups")\n\n    def test_invalid_unit(self):\n        with self.assertRaises(ValueError):\n            PantryItem(name="Flour", quantity=2, unit="")\n\n        with self.assertRaises(ValueError):\n            PantryItem(name="Flour", quantity=2, unit=123)